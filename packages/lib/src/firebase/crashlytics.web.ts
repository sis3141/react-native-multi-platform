export const setCrashlyticsAttribute = async (signObj: SignInfo) => {
  return;
};

export const CrashLogger = {
  helperLog: (text: string) => {
    return;
  },
  errorLog: (error: Error, jsErrorName: string) => {
    //todo add some log system for web
    return;
  },
};
